let items=[];let currentType="";const $=s=>document.querySelector(s);function esc(v=""){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}async function loadIndex(){const r=await fetch("../../knowledge/index/knowledge.json");if(!r.ok)throw Error("Không tải được Knowledge Base.");const d=await r.json();items=d.items||[]}function label(t){return {animal:"Vật nuôi",plant:"Cây trồng",aquaculture:"Thủy sản",standard:"Tiêu chuẩn"}[t]||"Khác"}function stats(){$("#count-all").textContent=items.length;$("#count-animal").textContent=items.filter(x=>x.knowledgeType==="animal").length;$("#count-plant").textContent=items.filter(x=>x.knowledgeType==="plant").length;$("#count-standard").textContent=items.filter(x=>x.knowledgeType==="standard").length}function render(){const q=$("#search").value.trim().toLocaleLowerCase("vi"),f=items.filter(x=>{const t=[x.name,x.summary,x.category,x.knowledgeType].join(" ").toLocaleLowerCase("vi");return(!currentType||x.knowledgeType===currentType)&&(!q||t.includes(q))});$("#kb-list").innerHTML=f.length?f.map(x=>`<a class="kb-card" href="../../pages/knowledge/?path=${encodeURIComponent(x.path)}"><div class="icon">${esc(x.icon||"📚")}</div><h3>${esc(x.name)}</h3><p>${esc(x.summary||"")}</p><div class="kb-meta"><span>${esc(label(x.knowledgeType))}</span><span>${esc(x.category)}</span></div></a>`).join(""):'<div class="empty">Không tìm thấy đối tượng phù hợp.</div>'}function exportIndex(){const p={version:"3.0",updated:new Date().toISOString().slice(0,10),items};const b=new Blob([JSON.stringify(p,null,2)],{type:"application/json;charset=utf-8"}),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download="knowledge.json";a.click();URL.revokeObjectURL(u)}document.addEventListener("DOMContentLoaded",async()=>{try{await loadIndex();stats();render()}catch(e){alert(e.message)}document.querySelectorAll(".filter").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");currentType=b.dataset.type;render()}));$("#search").addEventListener("input",render);$("#btn-export-index").addEventListener("click",exportIndex);$("#btn-open-studio").addEventListener("click",()=>location.href="../object/")});