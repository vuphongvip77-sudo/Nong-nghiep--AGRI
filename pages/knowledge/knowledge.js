const p=new URLSearchParams(location.search),path=p.get("path")||"knowledge/animals/ga-sao.json";function esc(v=""){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}async function load(){const r=await fetch(`../../${path}`);if(!r.ok)throw Error("Không tải được dữ liệu tri thức.");return r.json()}function render(d){document.title=`${d.name} | AGRI`;document.querySelector("#knowledge-header").innerHTML=`<div class="knowledge-title"><div class="icon">${esc(d.icon||"📚")}</div><div><h1>${esc(d.name)}</h1><p class="lead">${esc(d.summary||"")}</p><div class="knowledge-meta">Phiên bản: ${esc(d.version||"3.0")} • Cập nhật: ${esc(d.updated||"")}</div></div></div>`;document.querySelector("#knowledge-nav").innerHTML=(d.sections||[]).map((s,i)=>`<a href="#${esc(s.id)}" class="${i===0?"active":""}"><span>${esc(s.icon||"•")}</span><span>${esc(s.title)}</span></a>`).join("");document.querySelector("#knowledge-content").innerHTML=(d.sections||[]).map(s=>`<section class="knowledge-section" id="${esc(s.id)}"><h2><span>${esc(s.icon||"•")}</span>${esc(s.title)}</h2>${(s.content||[]).map(x=>`<p>${esc(x)}</p>`).join("")}${(s.items||[]).length?`<ul>${s.items.map(x=>`<li>${esc(x)}</li>`).join("")}</ul>`:""}</section>`).join("")}load().then(render).catch(e=>{document.querySelector("#knowledge-content").innerHTML=`<div class="empty-state">${esc(e.message)}</div>`});